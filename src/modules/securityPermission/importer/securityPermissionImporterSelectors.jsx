import importerSelectors from '../../shared/importer/importerSelectors';

const securityPermissionImporterSelectors = importerSelectors(
  'securityPermission.importer',
);

export default securityPermissionImporterSelectors;
